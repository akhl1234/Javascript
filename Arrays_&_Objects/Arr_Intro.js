let Definition = "An ordered collection of items, similar to a list. Each item has a specific index, a numerical position starting from 0 that uniquely identifies its location within the array."
console.log(Definition);

const Declaration = ["index-1","index-2","strings",55,10.8] // always use brackets to store data
console.log(Declaration);
console.log(Declaration[2]);