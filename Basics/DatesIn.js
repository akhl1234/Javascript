const Now = new Date() //when you console this it will print right time..
const Year = Now.getFullYear(); // show current year
const months = Now.getMonth(); // January is 0, December is 11
const Day = Now.getUTCDate(); // current date of month
const Types_change=Now.toDateString(); //as a string representation