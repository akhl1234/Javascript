const What_func_call =
  "It’s like dialing a phone number to connect with someone—when you call a function, you activate its code and make it run! ";

function Add(a, b) {
  return a + b;
}

function Average_of_twoNum(a, b) {
  return (a + b) / 2;
}

let x = Average_of_twoNum(15, 35);
console.log(x);
